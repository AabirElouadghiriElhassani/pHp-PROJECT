{% extends 'base.html.twig' %}

{% block title %}Tableau de bord - Admin Cinema{% endblock %}

{% block body %}
<style>
    body {
        margin: 0;
        font-family: 'Segoe UI', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        background: radial-gradient(circle at top, #020617 0, #020617 40%, #000 100%);
        color: #e5e7eb;
    }
    .layout {
        min-height: 100vh;
        display: grid;
        grid-template-columns: 240px 1fr;
    }
    .sidebar {
        background: radial-gradient(circle at top left, #0f172a, #020617 70%);
        border-right: 1px solid #111827;
        padding: 24px 20px;
    }
    .sidebar-title {
        font-size: 18px;
        text-transform: uppercase;
        letter-spacing: .16em;
        margin-bottom: 24px;
    }
    .sidebar-title span { color: #facc15; }
    .sidebar-section {
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: .12em;
        color: #6b7280;
        margin-top: 16px;
        margin-bottom: 6px;
    }
    .sidebar-nav a {
        display: block;
        padding: 8px 10px;
        border-radius: 10px;
        color: #e5e7eb;
        font-size: 13px;
    }
    .sidebar-nav a.active {
        background: linear-gradient(90deg, #f97316, #facc15);
        color: #111827;
        font-weight: 600;
    }

    .content {
        padding: 20px 26px 32px;
    }
    .page-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 18px;
    }
    .page-header h1 {
        font-size: 22px;
        margin: 0;
    }
    .page-header p {
        font-size: 13px;
        color: #9ca3af;
    }

    .filters {
        display: flex;
        gap: 10px;
        align-items: center;
        margin-bottom: 18px;
        font-size: 13px;
    }
    .filters a {
        padding: 5px 10px;
        border-radius: 999px;
        border: 1px solid #374151;
        color: #e5e7eb;
    }
    .filters a.active {
        background: #f97316;
        border-color: #f97316;
        color: #111827;
        font-weight: 600;
    }

    .cards {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px,1fr));
        gap: 14px;
        margin-bottom: 20px;
    }
    .card {
        background: radial-gradient(circle at top, #111827, #020617 70%);
        border-radius: 16px;
        padding: 14px 14px 12px;
        border: 1px solid #111827;
        box-shadow: 0 15px 30px rgba(0,0,0,0.7);
        font-size: 13px;
    }
    .card-title {
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: .12em;
        color: #9ca3af;
        margin-bottom: 4px;
    }
    .card-value {
        font-size: 20px;
        font-weight: 600;
    }
    .card-sub {
        font-size: 11px;
        color: #9ca3af;
    }

    .grid-2 {
        display: grid;
        grid-template-columns: minmax(0,2.1fr) minmax(0,1.9fr);
        gap: 16px;
        margin-bottom: 18px;
    }
    .panel {
        background: radial-gradient(circle at top, #020617, #020617 70%);
        border-radius: 16px;
        border: 1px solid #111827;
        box-shadow: 0 15px 30px rgba(0,0,0,0.7);
        padding: 14px 14px 10px;
        font-size: 13px;
    }
    .panel-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 8px;
    }
    .panel-header h2 {
        font-size: 14px;
        text-transform: uppercase;
        letter-spacing: .12em;
    }
    .panel-header a {
        font-size: 12px;
        color: #a5b4fc;
    }
    table {
        width: 100%;
        border-collapse: collapse;
        font-size: 12px;
    }
    th, td {
        padding: 6px 4px;
        text-align: left;
    }
    th {
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: .08em;
        color: #9ca3af;
        border-bottom: 1px solid #111827;
    }
    tr:nth-child(even) td {
        background: rgba(15,23,42,0.7);
    }
    .text-right { text-align: right; }
    .badge {
        display: inline-block;
        padding: 2px 6px;
        border-radius: 999px;
        font-size: 10px;
        text-transform: uppercase;
        letter-spacing: .1em;
    }
    .badge-hot {
        background: rgba(248,113,22,0.2);
        color: #fed7aa;
    }
    .badge-calm {
        background: rgba(55,65,81,0.8);
        color: #d1d5db;
    }

    .panel-small {
        font-size: 12px;
    }
</style>

<div class="layout">
    <aside class="sidebar">
        <div class="sidebar-title">Galaxy <span>Cinema</span></div>

        <div class="sidebar-section">Tableaux de bord</div>
        <div class="sidebar-nav">
            <a href="{{ path('app_admin_stat', {periode: 'all'}) }}" class="active">Vue globale</a>
        </div>

        <div class="sidebar-section">Administration</div>
        <div class="sidebar-nav">
            <a href="{{ path('app_user_index') }}">Gestion des utilisateurs</a>
            <a href="{{ path('app_films') }}">Gestion des films</a>
            <a href="{{ path('app_salle') }}">Gestion des salles</a>
        </div>
    </aside>

    <main class="content">
        <div class="page-header">
            <div>
                <h1>Tableau de bord statistique</h1>
                <p>
                    L’administrateur général ne gère pas les réservations, il supervise les ventes,
                    l’occupation des salles et les films les plus réservés.
                </p>
            </div>
        </div>

        <div class="filters">
            <span>Période :</span>
            <a href="{{ path('app_admin_stat', {periode: 'all'}) }}" class="{{ periode == 'all' ? 'active' : '' }}">Tout</a>
            <a href="{{ path('app_admin_stat', {periode: 'month'}) }}" class="{{ periode == 'month' ? 'active' : '' }}">30 derniers jours</a>
            <a href="{{ path('app_admin_stat', {periode: 'week'}) }}" class="{{ periode == 'week' ? 'active' : '' }}">7 derniers jours</a>
        </div>

        <div class="cards">
            <div class="card">
                <div class="card-title">Chiffre d’affaires total</div>
                <div class="card-value">{{ ca_global|number_format(2, '.', ' ') }} MAD</div>
                <div class="card-sub">Toutes réservations confirmées sur la période choisie.</div>
            </div>
            <div class="card">
                <div class="card-title">Films suivis</div>
                <div class="card-value">{{ ca_par_film|length }}</div>
                <div class="card-sub">Nombre de films ayant au moins une réservation.</div>
            </div>
            <div class="card">
                <div class="card-title">Salles suivies</div>
                <div class="card-value">{{ occupation|length }}</div>
                <div class="card-sub">Salles prises en compte pour le taux d’occupation.</div>
            </div>
        </div>

        <div class="grid-2">
            <section class="panel">
                <div class="panel-header">
                    <h2>Films les mieux vendus</h2>
                    <a href="{{ path('app_admin_stat_csv') }}">Exporter CSV</a>
                </div>
                <table>
                    <thead>
                    <tr>
                        <th>Film</th>
                        <th class="text-right">Réservations</th>
                        <th class="text-right">CA (MAD)</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for row in ca_par_film %}
                        {% set film = row.film %}
                        <tr>
                            <td>
                                {{ film.titre }}
                                {% if loop.index <= 3 %}
                                    <span class="badge badge-hot">Top {{ loop.index }}</span>
                                {% endif %}
                            </td>
                            <td class="text-right">{{ row.nbReservations }}</td>
                            <td class="text-right">{{ row.ca|number_format(2, '.', ' ') }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="3">Aucune réservation pour le moment.</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </section>

            <section class="panel">
                <div class="panel-header">
                    <h2>Taux d’occupation des salles</h2>
                </div>
                <table>
                    <thead>
                    <tr>
                        <th>Salle</th>
                        <th class="text-right">Occupation</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for row in occupation %}
                        {% set salle = row[0] %}
                        {% set taux = row.placesOccupees and row.capacite ? (row.placesOccupees / row.capacite * 100)|round(1) : 0 %}
                        <tr>
                            <td>
                                {{ salle.nom }}
                                {% if taux > 80 %}
                                    <span class="badge badge-hot">Très demandée</span>
                                {% else %}
                                    <span class="badge badge-calm">Normale</span>
                                {% endif %}
                            </td>
                            <td class="text-right">{{ taux }} %</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="2">Aucune donnée d’occupation pour le moment.</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </section>
        </div>

        <section class="panel panel-small">
            <div class="panel-header">
                <h2>Résumé</h2>
            </div>
            <p>
                Ce tableau de bord offre au <strong>administrateur général</strong> une vision globale :
                chiffre d’affaires par période, classement des films par réservations et par CA, et
                pourcentage d’occupation des salles. Il n’intervient pas dans les réservations,
                il pilote l’activité à partir de ces indicateurs.
            </p>
        </section>
    </main>
</div>
{% endblock %}
