{% extends 'base.html.twig' %}

{% block title %}Inscription - Galaxy Cinema{% endblock %}

{% block body %}
<style>
    body {
        margin: 0;
        font-family: 'Segoe UI', system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
        background: radial-gradient(circle at top, #020617 0, #020617 40%, #000 100%);
        color: #e5e7eb;
    }
    a { text-decoration: none; }

    .page-wrapper {
        min-height: 100vh;
        display: flex;
        align-items: stretch;
    }
    .auth-hero {
        flex: 1.3;
        background-image: url('https://images.pexels.com/photos/799137/pexels-photo-799137.jpeg?auto=compress&cs=tinysrgb&w=1200');
        background-size: cover;
        background-position: center;
        position: relative;
    }
    .auth-hero::before {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, rgba(15,23,42,0.9), rgba(15,23,42,0.2));
    }
    .auth-hero-content {
        position: absolute;
        inset: 0;
        padding: 40px;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        color: #e5e7eb;
    }
    .logo {
        font-size: 26px;
        text-transform: uppercase;
        letter-spacing: .16em;
    }
    .logo span { color: #facc15; }
    .auth-hero-text {
        max-width: 380px;
    }
    .auth-hero-text h1 {
        font-size: 30px;
        margin-bottom: 12px;
    }
    .auth-hero-text p {
        font-size: 14px;
        color: #cbd5f5;
    }
    .auth-hero-footer {
        font-size: 12px;
        color: #9ca3af;
    }

    .auth-panel {
        flex: 1;
        padding: 40px 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: radial-gradient(circle at top, #020617, #020617 55%, #000 100%);
    }
    .auth-card {
        width: 100%;
        max-width: 420px;
        background: rgba(15,23,42,0.96);
        border-radius: 20px;
        padding: 28px 26px 24px;
        box-shadow: 0 25px 60px rgba(0,0,0,0.9);
        border: 1px solid #1f2937;
    }
    .auth-title {
        font-size: 22px;
        margin-bottom: 6px;
    }
    .auth-sub {
        font-size: 13px;
        color: #9ca3af;
        margin-bottom: 16px;
    }
    .flash-messages {
        margin-bottom: 12px;
    }
    .flash-success {
        background: rgba(22,163,74,0.12);
        border: 1px solid rgba(34,197,94,0.35);
        color: #bbf7d0;
        padding: 8px 10px;
        border-radius: 10px;
        font-size: 13px;
    }

    form .form-group {
        margin-bottom: 12px;
    }
    label {
        font-size: 13px;
        color: #9ca3af;
        margin-bottom: 4px;
        display: block;
    }
    input[type="text"],
    input[type="email"],
    input[type="password"] {
        width: 100%;
        padding: 9px 12px;
        border-radius: 10px;
        border: 1px solid #374151;
        background: #020617;
        color: #f9fafb;
        font-size: 13px;
    }
    input::placeholder { color: #6b7280; }

    .form-error-message {
        color: #fca5a5;
        font-size: 11px;
        margin-top: 2px;
    }
    .btn-submit {
        width: 100%;
        margin-top: 10px;
        border-radius: 999px;
        border: none;
        padding: 10px 18px;
        font-weight: 600;
        font-size: 14px;
        cursor: pointer;
        background: linear-gradient(90deg, #f97316, #facc15);
        color: #111827;
        box-shadow: 0 15px 30px rgba(248,113,22,0.45);
    }
    .btn-submit:hover {
        background: linear-gradient(90deg, #fdba74, #facc15);
    }
    .auth-alt {
        margin-top: 12px;
        font-size: 12px;
        color: #9ca3af;
        text-align: center;
    }
    .auth-alt a {
        color: #a5b4fc;
    }

    @media (max-width: 900px) {
        .page-wrapper {
            flex-direction: column;
        }
        .auth-hero {
            display: none;
        }
        .auth-panel {
            padding: 24px 16px;
        }
        .auth-card {
            box-shadow: none;
        }
    }
</style>

<div class="page-wrapper">
    <div class="auth-hero">
        <div class="auth-hero-content">
            <div class="logo">Galaxy <span>Cinema</span></div>

            <div class="auth-hero-text">
                <h1>Votre compte cinéma en quelques secondes.</h1>
                <p>
                    Créez un compte pour réserver vos places, retrouver votre historique
                    de séances et profiter d’une expérience fluide, du choix du film jusqu’au paiement.
                </p>
            </div>

            <div class="auth-hero-footer">
                Un administrateur général supervise les statistiques, tandis que vous profitez du spectacle.
            </div>
        </div>
    </div>

    <div class="auth-panel">
        <div class="auth-card">
            <div class="auth-title">Inscription</div>
            <div class="auth-sub">
                Remplissez le formulaire pour rejoindre la plateforme de réservation Galaxy Cinema.
            </div>

            <div class="flash-messages">
                {% for label, messages in app.flashes %}
                    {% for message in messages %}
                        <div class="flash-{{ label }}">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            </div>

            {{ form_start(registrationForm) }}

                <div class="form-group">
                    {{ form_label(registrationForm.firstName) }}
                    {{ form_widget(registrationForm.firstName, {'attr': {'placeholder': 'Votre prénom'}}) }}
                    {{ form_errors(registrationForm.firstName) }}
                </div>

                <div class="form-group">
                    {{ form_label(registrationForm.lastName) }}
                    {{ form_widget(registrationForm.lastName, {'attr': {'placeholder': 'Votre nom'}}) }}
                    {{ form_errors(registrationForm.lastName) }}
                </div>

                <div class="form-group">
                    {{ form_label(registrationForm.email) }}
                    {{ form_widget(registrationForm.email, {'attr': {'placeholder': 'vous@example.com'}}) }}
                    {{ form_errors(registrationForm.email) }}
                </div>

                <div class="form-group">
                    {{ form_label(registrationForm.plainPassword.first) }}
                    {{ form_widget(registrationForm.plainPassword.first, {'attr': {'placeholder': 'Mot de passe'}}) }}
                    {{ form_errors(registrationForm.plainPassword.first) }}
                </div>

                <div class="form-group">
                    {{ form_label(registrationForm.plainPassword.second) }}
                    {{ form_widget(registrationForm.plainPassword.second, {'attr': {'placeholder': 'Répétez le mot de passe'}}) }}
                </div>

                <button type="submit" class="btn-submit">
                    Créer mon compte
                </button>

            {{ form_end(registrationForm) }}

            <div class="auth-alt">
                Vous avez déjà un compte ?
                <a href="{{ path('app_login') }}">Se connecter</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
