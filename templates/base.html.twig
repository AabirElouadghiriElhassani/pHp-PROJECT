<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Cinema Platform{% endblock %}</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;500;700&display=swap" rel="stylesheet">

    <style>
        body {
            background: linear-gradient(180deg, #0a0d14, #05070c);
            color: #f1f1f1;
            font-family: 'Manrope', sans-serif;
        }

        .navbar {
            background: rgba(0,0,0,0.6);
            backdrop-filter: blur(12px);
        }

        .brand {
            font-weight: 700;
            letter-spacing: 1px;
            font-size: 20px;
        }

        .accent {
            color: #c9a24d;
        }

        .glass {
            background: rgba(255,255,255,0.05);
            backdrop-filter: blur(16px);
            border-radius: 18px;
            box-shadow: 0 30px 60px rgba(0,0,0,0.5);
        }

        .btn-lux {
            background: linear-gradient(135deg, #c9a24d, #f1dc9a);
            color: #000;
            border: none;
            font-weight: 600;
            padding: 10px 22px;
            border-radius: 25px;
        }

        .muted {
            color: #9aa0b4;
        }
    </style>
</head>

<body>

<nav class="navbar py-3">
    <div class="container">
        <span class="brand">CINEMA<span class="accent">.</span></span>

        <div class="ms-auto">
            <a href="/users" class="text-light me-4 text-decoration-none">Users</a>
            <a href="/reservations" class="text-light me-4 text-decoration-none">Movies</a>
            <a href="/login" class="text-light text-decoration-none">Login</a>
        </div>
    </div>
</nav>

<div class="container my-5">
    {% block body %}{% endblock %}
</div>

</body>
</html>
